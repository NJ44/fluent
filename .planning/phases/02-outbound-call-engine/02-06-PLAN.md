---
phase: 02-outbound-call-engine
plan: "06"
type: execute
wave: 4
depends_on: ["02-04", "02-05"]
files_modified: []
autonomous: false
requirements:
  - CALL-01
  - CALL-02
  - CALL-03
  - CALL-06

must_haves:
  truths:
    - "A real outbound call is placed and rings a test phone number"
    - "The TCPA disclosure preamble fires as the FIRST utterance — before any task content"
    - "Call lifecycle transitions are visible in the UI (initiating → ringing → active → ended)"
    - "Post-call outcome summary appears within 30s of call ending"
    - "Full transcript is stored and readable in CallDetail"
  artifacts:
    - path: "supabase/migrations/20260504000004_create_calls.sql"
      provides: "calls table applied to production Supabase project"
      contains: "calls"
    - path: "supabase/migrations/20260504000005_add_retell_agent_to_voice_clones.sql"
      provides: "voice_clones retell columns applied"
      contains: "retell_agent_id"
  key_links:
    - from: "Dashboard form"
      to: "initiate-call Netlify function"
      via: "POST /.netlify/functions/initiate-call"
      pattern: "initiate-call"
    - from: "retell-webhook Netlify function"
      to: "calls table"
      via: "status updates on call lifecycle events"
      pattern: "from\\('calls'\\)"
---

<objective>
Human verification checkpoint: apply DB migrations to production Supabase, set all required env vars in Netlify, and conduct a live outbound call test to verify end-to-end: TCPA disclosure fires first, call lifecycle tracks correctly, post-call summary is generated by Claude.

Purpose: No amount of unit testing validates that Retell dials a real phone number, the TCPA preamble fires before the task content, and Claude summarization completes within Netlify's timeout. These require a live test.
Output: Confirmed live call end-to-end with TCPA disclosure, lifecycle tracking, and Claude summary.
</objective>

<execution_context>
@C:/Users/Asus/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Asus/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-outbound-call-engine/02-RESEARCH.md
@.planning/phases/02-outbound-call-engine/02-05-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-action">
  <name>Task 1: Apply DB migrations + configure Netlify env vars</name>
  <what-to-do>
Before the live call test, two manual setup steps are required that Claude cannot automate without Supabase CLI authentication and Netlify dashboard access.

**Step 1 — Apply Supabase migrations:**

Option A (Supabase CLI — preferred):
```bash
cd C:\Users\Asus\Desktop\Fluent
npx supabase db push
```

Option B (Supabase dashboard — fallback):
1. Open app.supabase.com → your Fluent project → SQL Editor
2. Run the contents of `supabase/migrations/20260504000004_create_calls.sql`
3. Run the contents of `supabase/migrations/20260504000005_add_retell_agent_to_voice_clones.sql`

Verify: In Supabase Table Editor, confirm `calls` table exists with all columns, and `voice_clones` table has `retell_agent_id` and `retell_llm_id` columns.

**Step 2 — Set Netlify env vars:**

In Netlify dashboard → Site settings → Environment variables, confirm these are set:
- `RETELL_API_KEY` — your Retell API key (already set from Phase 1)
- `RETELL_FROM_NUMBER` — your Retell-purchased outbound number (E.164, e.g. +15005550006)
- `ANTHROPIC_API_KEY` — your Anthropic API key

Also verify that the Retell webhook URL is reachable:
- `{your-netlify-url}/.netlify/functions/retell-webhook`
- This URL must be accessible from the internet (not localhost) for Retell to deliver webhooks

**Step 3 — Purchase/configure Retell outbound number (if not yet done):**
1. Log in to app.retellai.com
2. Go to Phone Numbers → Buy Number
3. Purchase a US number for outbound calls
4. Note the E.164 format (e.g. +15005550006) and set `RETELL_FROM_NUMBER` in Netlify

Type "done" when migrations are applied and env vars are set.
  </what-to-do>
  <resume-signal>Type "done" when Supabase migrations are applied and Netlify env vars are set</resume-signal>
</task>

<task type="checkpoint:human-verify">
  <name>Task 2: Live call end-to-end verification</name>
  <what-built>
Complete Phase 2 outbound call engine:
- IntentForm → POST /initiate-call → Retell createPhoneCall → webhook → calls table → CallDetail
- TCPA disclosure hardcoded in Retell LLM begin_message (fires before task content)
- Lifecycle: initiating → ringing → active → ended → analyzed
- Claude claude-haiku-4-5 post-call summarization (async, non-blocking)
  </what-built>
  <how-to-verify>
**Test 1 — Full call flow (CALL-01, CALL-02, CALL-05):**
1. Sign in to your Fluent app (deployed or local with Netlify CLI)
2. Navigate to /dashboard
3. Fill in the form:
   - Call goal: "Ask if they have any appointments available this week"
   - Phone number: your own mobile number (E.164 format)
   - Fallback: "If no appointments this week, ask about next week"
   - Check the consent box
4. Click "Send call"
5. Your phone should ring within 5-10 seconds

**Verify on your phone:**
- [ ] Phone rings (CALL-01)
- [ ] First words you hear are the TCPA disclosure: "Hi, this is an AI assistant placing this call on behalf of..." (CALL-02, CALL-03)
- [ ] Disclosure appears BEFORE any task content about appointments (CALL-02)
- [ ] AI proceeds to the task after the disclosure

**Test 2 — Lifecycle tracking (CALL-05):**
6. While your phone rings, observe /calls/:id in the browser:
   - [ ] Status shows "Ringing" while phone rings
   - [ ] Status changes to "Call in progress" when you answer
   - [ ] Status changes to "Call completed" after you hang up

**Test 3 — Post-call summary (POST-01, POST-02):**
7. After call ends, wait 15-30 seconds on the CallDetail page
   - [ ] Outcome summary appears (e.g. "User inquired about appointments; no specific slot confirmed.")
   - [ ] Full transcript is visible showing the conversation

**Test 4 — Failure state (CALL-07):**
8. Submit a call to a number you know won't answer (or a deliberately invalid number)
   - [ ] UI shows a human-readable error message ("No answer", etc.)
   - [ ] App does NOT crash or show a generic "500 error"

**Test 5 — Call history (HIST-01, HIST-02):**
9. Navigate to /history
   - [ ] Both calls appear in the list with intent and status
   - [ ] Clicking either row opens its CallDetail page

**TCPA Latency check (CALL-06):**
On the live test call, time from when you say your first word to when the AI responds.
- [ ] AI response latency feels < 1.5 seconds (subjective — mark PASS if it feels natural)

Note the following in your sign-off:
1. Did the TCPA disclosure fire as the FIRST utterance? (Yes/No)
2. Did the call lifecycle track correctly in the UI? (Yes/No)
3. Was a post-call summary generated? (Yes — what did it say? / No — check Netlify function logs)
4. AI response latency during the call: (felt fast / felt slow)
  </how-to-verify>
  <resume-signal>Describe what you observed: TCPA preamble (yes/no), lifecycle tracking (yes/no), summary generated (yes + text / no), latency (fast/slow). Type "approved" if all checks pass, or describe any issues.</resume-signal>
</task>

</tasks>

<verification>
This plan is verified by the human checkpoint itself. Phase 2 is complete when:
1. All automated tests pass: `npx vitest run` exits green
2. Live call placed: phone rings, TCPA preamble fires first, task content follows
3. Lifecycle transitions visible in /calls/:id UI
4. Post-call summary generated by Claude and stored in calls table
5. CallHistory shows both test calls with correct intents and outcomes
</verification>

<success_criteria>
- CALL-01: Live outbound call placed and connected via Retell
- CALL-02: TCPA disclosure fires as first utterance — no task content before disclosure
- CALL-03: Webhook signature verification confirmed (no 401 errors in Netlify logs)
- CALL-06: AI response latency felt < 1.5s during live call
- Phase 2 complete: user approved human checkpoint
</success_criteria>

<output>
After completion, create `.planning/phases/02-outbound-call-engine/02-06-SUMMARY.md`
</output>
